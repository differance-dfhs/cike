# 此刻产品合同

## 产品定位

此刻不是通知中心、待办列表或第二个聊天 Agent。它是 Codex 的主动感知和介入层。后台可以持续理解、检索、核验和准备本地成果，灵动岛只负责交付已经产生的价值或请用户拍板。

北极星指标是：

> 用户尚未输入 query 前，此刻已经交付一个被采用的结果。

## Silence Gate

任何候选介入在进入界面前必须依次通过：

1. **来源有效**：妙记必须读到正文或逐字稿，不用日程标题补写事实。
2. **责任明确**：只接受本人承诺、明确指派、已到本人验收节点的事项。
3. **仍未完成**：飞书 Todo 已完成，明确完成、取消或无需的消息立即归档。Codex 生成草稿不等于业务任务完成。
4. **时效有效**：普通消息最多 24 小时，项目改动最多 48 小时，节奏建议在当前状态结束时立即失效。会后本人 Todo 持续到完成、取消或范围被替代。
5. **跨源去重**：使用“项目 + 责任人 + 规范化目标 + 交付物”形成 Goal Key。群聊、妙记、Todo 和 Chronicle 的同一目标只能形成一个介入对象。
6. **有价值增量**：至少已有可消费成果、新决策信息、新风险或时效性判断之一。“收到消息”、“会议结束”、“有一个 Todo”都不是价值。
7. **介入时机合适**：会议、演示和深度专注期间不弹普通结果。结果就绪后等待会后、任务切换或空闲点。

不通过的候选必须有可审计的静默原因，但原始事件不进入灵动岛。

## 不同状态的前台行为

| 状态 | 前台行为 |
|---|---|
| 静默忽略 | 不显示，不启动 Codex |
| 后台准备 | 收起态允许一行短进度，不弹结果卡 |
| 建议就绪 | 在合适时机 Glance，展开后直接读结论 |
| 工作完成 | 显示完成时间、验证和可点击本地产物 |
| 需要拍板 | 给出推荐项、代价和由当前事件生成的选项 |
| 失败 | 明确显示未完成，不能伪装成结果 |

## 通用任务交付合同

论文和方案只是验收样例，不是任务分类。任何通过 Silence Gate 的明确任务都走同一条闭环：

1. 把多源信号归一为目标、责任人、完成条件、权限上限和期望交付物。
2. Codex 在后台完成实际工作；普通任务默认形成可在此刻内读取的原生结果。
3. 需要专用体验时，由可插拔 Delivery Adapter 准备并校验交付物，例如论文阅读包、飞书文档、本地文件或后续新增工具。
4. 只有宿主校验成功后才能签发不含路径、URL 或凭据的 opaque Delivery Reference；模型不能生成可点击目标。
5. 灵动岛按钮使用交付记录中的动态动作语义。动作数量不设固定上限，界面高度随当前情境的必要选项自然展开；点击后直接获得已完成结果，不再要求用户重复输入 query、选择来源或等待准备。
6. 失败、超时、内容损坏或回读不一致时不显示“已就绪”，保留本地结果并给出真实失败状态。

下面两条黄金链路用于验证通用合同和专用交付适配器，不限制此刻可处理的任务范围。核对数据、审阅材料、梳理会议 Todo、比较版本、生成本地成果或在授权项目内执行可逆改动，都必须复用同一套 Work Order、Receipt、Delivery Reference 和 Intervention 流程。

## 黄金链路：论文阅读

“会后要看主动 Agent 论文”必须按以下链路工作：

1. 会议结束后读取飞书妙记正文。
2. 确认这是用户本人的未完成任务，且不存在相同已看过或正在运行的研究 Goal。
3. 以 L2 静默准备启动 Codex，不先通知用户。
4. Codex 检索一手来源，去重后筛选最值得读的内容，生成摘要、关键结论、对当前产品和评测的启发及来源。
5. 在产物完成前，灵动岛最多显示“正在筛论文”。
6. 产物就绪并等到合适时机后，Glance 显示“已替你筛好主动 Agent 论文”。
7. 点击“阅读论文”后直接打开本机 DeepRead，原文、中文对照和首段均已加载完成。
8. 阅读或采纳只收起建议卡。飞书 Todo 是否完成仍由真实任务状态决定。

## 黄金链路：方案文档

1. 明确的本人任务要求产出方案，且用户已开启“允许把完成方案发布到我的飞书文档”。
2. Codex 先只读核对资料、写完整正文并自校对，不把提纲或过程日志当成果。
3. 宿主仅在用户的飞书「我的文档库」创建文档，并立即回读标题、正文锚点与唯一交付标识。
4. 回读通过后才显示“打开方案”；点击直接打开已验证文档。
5. 未开启授权、认证失效或回读失败时，只保留本地结果，不创建或伪造远端交付。

## 必测用例

- 会后妙记包含“看主动 Agent 论文”：只启动一个研究 Work Order，最终只交付一个结果。
- 飞书只是交流或同事汇报自己进度：无 Codex Job，无灵动岛内容。
- Skill 数量变化：旧范围不再显示，Codex 核对影响后交付差异或修改稿。
- 任务已完成、取消、已看过、不重要、重复或过期：重扫和重启后仍不出现。
- 需要共享写回、发送、发布或删除：可以先准备本地草稿，对外动作必须用户批准。
- 任意普通任务：Codex 完成后出现由任务语义生成的结果入口（例如“看范围差异”或“查看审阅结论”），并在此刻内读取，不能退化成只针对论文/方案的分支。
